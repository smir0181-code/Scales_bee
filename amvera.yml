meta:
  environment: python
  toolchain:
    name: pip
    version: "3.8"
build:
  requirementsPath: requirements.txt
run:
  command: waitress-serve --host=0.0.0.0 --port=5000 main:app
  containerPort: 5000